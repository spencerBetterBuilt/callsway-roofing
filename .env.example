# Copy this file to .env and fill in the value below to run
# Scripts/sync-google-reviews.py locally. .env is gitignored — this
# variable also needs to be added as a GitHub Actions secret
# (Settings > Secrets and variables > Actions) named the same thing
# for the scheduled workflow to run in CI.

GOOGLE_PLACES_API_KEY=
